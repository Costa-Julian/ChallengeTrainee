server:
  port: 8080
---
spring:
  profiles: default-postgres
  datasource:
    url: 'jdbc:postgresql://localhost:5432/juanperez'
    driver-class-name: 'org.postgresql.Driver'
    username: 'sarasa'
    password: 'sarasa'
    test-on-borrow: true
    validation-query: 'SELECT 1'
    log-validation-errors: true
    max-active: 20
    initial-size: 4
    autoReconnect: true
    autoReconnectForPools: true
    is-connection-validation-required: true
hibernate:
  hbm2ddlAuto: 'create-drop'
  showSql: true
---
spring:
  profiles: production
  jpa:
    daqtabase-platform: org.hibernate.dialect.PostgreSQL94Dialect
  datasource:
    #          url: 'jdbc:postgresql://localhost:5432/basededatos'
    driver-class-name: 'org.postgresql.Driver'
    username: 'root'
    password: 'root'
    test-on-borrow: true
    validation-query: 'SELECT 1'
    log-validation-errors: true
    max-active: 20
    initial-size: 4
    autoReconnect: true
    autoReconnectForPools: true
    is-connection-validation-required: true
hibernate:
  hbm2ddlAuto: 'create'
  showSql: true